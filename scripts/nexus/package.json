{
  "name": "nexus-npm-publish",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "build": "npx ../../node_modules/.bin/tsc -p tsconfig.json -w"
  },
  "private": true,
  "dependencies": {
    "replace": "^1.1.0"
  },
  "peerDependencies": {
    "replace": "^1.1.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=10.0.0",
    "npm": ">=6.0.0"
  }
}